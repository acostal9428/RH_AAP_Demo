---
- name: Reboot Windows machines
  hosts: windows
  tasks:
    - name: Example from an Ansible Playbook
      ansible.windows.win_ping:
      # win_reboot:
      #   reboot_timeout: 600  # Optional, sets the timeout in seconds
      #   msg: "Rebooting the system via Ansible"  # Optional, sets a message shown on the screen before reboot

